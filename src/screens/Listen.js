import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

const Listen = () => {
  return (
    <View style={styles.container}>
      <Text>Listen page is under development !</Text>
    </View>
  );
};

export default Listen;

const styles = StyleSheet.create({
  container: {
    alignItems: 'center',
    paddingVertical: '50%',
  },
});
